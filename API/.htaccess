RewriteEngine On

# If the request is for an existing file or directory, don't touch it
RewriteCond %{REQUEST_FILENAME} -f [OR]
RewriteCond %{REQUEST_FILENAME} -d
RewriteRule ^ - [L]

# Everything else goes to API/index.php
RewriteRule ^ API/index.php [L]